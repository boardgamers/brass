export enum MajorPhase {
  CanalPhase = "canal",
  RailPhase = "rail"
};

export enum RoundPhase {
  GameSetup = "gamesetup",
  PeriodSetup= "periodsetup",
  RoundSetup = "roundsetup",
  PlayCards = "cards",
  VP = "vp"
}
